// Tailwind class string for NodeTooltip component
export const tooltipClasses = 'font-inter text-[13px] p-4 bg-milk-base/[0.04] text-milk-base rounded-lg border border-milk-base/20 shadow-lg backdrop-blur-[10.4px] min-w-[200px]';

// For cases where inline styles are still needed (e.g., dynamic positioning)
export const defaultStyles = {
  fontFamily: "'Inter', sans-serif"
};